# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a repository-specific `AGENTS.md` at the root of the project for Codex-first collaboration.

**Architecture:** The implementation is documentation-only. First capture scope and structure in a short design spec, then generate a single operational `AGENTS.md` that reflects the current frontend/backend split, startup flow, verification flow, and known pitfalls.

**Tech Stack:** Markdown, existing repository docs, Vue 3 + Vite frontend conventions, Spring Boot 3 backend conventions

---

### Task 1: Capture design scope

**Files:**
- Create: `docs/superpowers/specs/2026-03-30-agents-md-design.md`

- [ ] **Step 1: Write the design summary**

```md
# AGENTS.md Design

## Goal

Create a root-level `AGENTS.md` for this repository that is optimized for Codex-first collaboration.
```

- [ ] **Step 2: Verify the spec file exists and includes the agreed scope**

Run:

```bash
Get-Content E:\me\myweb\docs\superpowers\specs\2026-03-30-agents-md-design.md
```

Expected:

- file exists
- it includes goal, scope, structure, repository facts, and acceptance criteria

### Task 2: Write the root-level AGENTS file

**Files:**
- Create: `AGENTS.md`
- Reference: `README.md`
- Reference: `PLAN.md`
- Reference: `TODO.md`
- Reference: `frontend/package.json`
- Reference: `frontend/src/api/http.ts`
- Reference: `frontend/vite.config.ts`
- Reference: `frontend/src/router/index.ts`
- Reference: `backend/pom.xml`
- Reference: `backend/src/main/resources/application.yml`

- [ ] **Step 1: Draft the operational sections**

```md
## Purpose
## Requirement Sources
## Repository Map
## Local Runbook
## Frontend Rules
## Backend Rules
## API and Data Contract Notes
## Documentation and Checklist Rules
## Verification
## Known Project Pitfalls
## Editing Guardrails
## Preferred Codex Workflow in This Repo
```

- [ ] **Step 2: Fill each section with current repository facts**

```md
- Frontend dev server: `9080`
- Frontend API base: `/api`
- Backend Swagger: `/api/swagger-ui.html`
- Backend Java requirement: `JDK 17+`
```

- [ ] **Step 3: Verify the file content**

Run:

```bash
Get-Content E:\me\myweb\AGENTS.md
```

Expected:

- file exists at root next to `README.md`
- content is repository-specific
- no generic placeholder text remains

### Task 3: Final verification

**Files:**
- Verify: `AGENTS.md`
- Verify: `docs/superpowers/specs/2026-03-30-agents-md-design.md`
- Create: `docs/superpowers/plans/2026-03-30-agents-md.md`

- [ ] **Step 1: Confirm all documentation files are present**

Run:

```bash
Get-ChildItem E:\me\myweb\docs\superpowers\specs
Get-ChildItem E:\me\myweb\docs\superpowers\plans
Get-ChildItem E:\me\myweb\AGENTS.md
```

Expected:

- all three files are present

- [ ] **Step 2: Self-review for stale or incorrect instructions**

Checklist:

- does `AGENTS.md` mention the frontend/backend split
- does it mention `README.md`, `PLAN.md`, `TODO.md`
- does it mention JDK 17 requirement
- does it mention Swagger path
- does it mention Vite stale process / malformed `.vue` pitfalls

- [ ] **Step 3: Report completion**

State:

- root `AGENTS.md` created
- design and plan files created
- content matches current repository structure and conventions
